[build]
builder = "DOCKERFILE"
dockerfilePath = "apps/api/Dockerfile"

[deploy]
startCommand = "sh -c \"PYTHONPATH=/app/src exec uvicorn main:app --host 0.0.0.0 --port $PORT\""
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
