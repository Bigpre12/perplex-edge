# Web framework
fastapi>=0.109.0
uvicorn[standard]>=0.30.0

# Database (async)
sqlalchemy[asyncio]>=2.0.25
asyncpg>=0.29.0
alembic>=1.13.0
psycopg2-binary>=2.9.0  # For compatibility with some tools

# Configuration
pydantic-settings>=2.1.0
PyYAML>=6.0.0

# HTTP client (for external API calls)
httpx>=0.26.0

# Testing & Code Quality
pytest>=8.0.0
pytest-asyncio>=0.23.0
mypy>=1.8.0
ruff>=0.1.0

# Monitoring & Error Tracking
sentry-sdk[fastapi]>=1.40.0

# Structured Logging
structlog>=24.1.0
python-json-logger>=2.0.7

# Caching
redis>=5.0.0

# Math/Statistics
scipy>=1.12.0  # Required for probability calibration in grading router
scikit-learn>=1.4.0  # Required for IsotonicRegression in probability calibration

# Resilience (retries, circuit breakers)
tenacity>=8.2.0
pybreaker>=1.0.0

# Validation
email-validator>=2.0.0
