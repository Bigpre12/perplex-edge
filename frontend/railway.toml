# Railway configuration for Perplex Edge Frontend
[build]
builder = "DOCKERFILE"

[deploy]
healthcheckPath = "/"
healthcheckTimeout = 100
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

# Static file serving
[variables]
NODE_ENV = "production"
